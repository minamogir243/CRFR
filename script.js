// リダイレクト先のURLを指定
const redirectUrl = "https://sites.google.com/view/crystal-frontier/Home";  // リダイレクト先のURLを指定してください

// 3秒後にリダイレクト
setTimeout(() => {
    window.location.href = redirectUrl;
}, 3000);
